---
description: Compaction 压缩链路不变量约束 — 修改压缩相关代码时必须遵守
globs:
  - src/shared/agent/process-manager.ts
  - src/mainview/stores/agent-event-handler.ts
  - src/mainview/stores/use-chat-store.ts
  - src/mainview/lib/message-mapper.ts
  - src/mainview/lib/turn-aggregator.ts
---

# Compaction 不变量约束

> ESLint 规则 `rpc/compaction-entries-sync` 和 `rpc/compaction-reload-pairing` 会在 CI 中自动检查以下不变量。本文档补充说明设计原因和修改指引。

## 不变量 1: JSONL 解析双路径同步

**文件**: `src/shared/agent/process-manager.ts` → `getFullMessages()`

`getFullMessages` 有两个 JSONL 解析分支：
1. **Sandbox 路径**（`isSandboxSessionPath && globalSandboxManager`）
2. **本地路径**（`resolvedSessionPath && existsSync`）

**规则**: 每个分支中 `parsed.type === "compaction"` 必须同时：
- push 到 `allCompactionEntries`（用于 streaming dedup）
- push 到 `allMessages`（注入 `{ role: "compactionSummary" }`，保持 JSONL 时间顺序）

**为什么**: 如果只 push `allCompactionEntries` → 刷新后 compactionSummary 消失。如果只 push `allMessages` → streaming 时出现重复 compactionSummary。

**ESLint**: `rpc/compaction-entries-sync` — error

## 不变量 2: compactionDeferredSessions 配对

**文件**: `src/mainview/stores/agent-event-handler.ts`

`compactionDeferredSessions: Set<string>` 跟踪 streaming 中被压缩的 session。

**规则**:
- `compaction_end` 在 `isActivelyStreaming=true` 时必须 `add(sessionId)` 到 `compactionDeferredSessions`
- `agent_end` 必须检查 `compactionDeferredSessions.has(sessionId)` → `delete` → `loadSessionMessages({ force: true })`

**为什么**: 如果 `compaction_end` 跳过 force reload 但不记录补偿，streaming 结束后消息永远不同步。如果 `agent_end` 不检查补偿 Set，补偿永远不会执行。

**ESLint**: `rpc/compaction-reload-pairing` — error

## 不变量 3: Streaming merge dedup 精确性

**文件**: `src/shared/agent/process-manager.ts` → streaming merge 块

**规则**: compactionSummary 的 dedup 必须基于 entryId 精确匹配，不能用 `allCompactionEntries.length > 0` 全局检查。

```typescript
// ✅ 正确: entryId 精确匹配
const compactionEntryIds = new Set(allCompactionEntries.map((c) => c.entryId));
if (role === "compactionSummary") {
  if (eid && compactionEntryIds.has(eid)) continue;
  if (!eid && filteredHasCompaction) continue;
}

// ❌ 错误: 全局检查（会误跳其他分支的 compaction）
if (role === "compactionSummary" && allCompactionEntries.length > 0) continue;
```

**ESLint**: 无自动检查（需人工 code review）

## 不变量 4: branch filtering 包含 compaction 节点

**文件**: `src/shared/agent/process-manager.ts` → `getFullMessages()`

compaction entry 在 `parentById` 中有 parentId，是消息树的一部分。branch filtering 从 leaf 追踪到 root 时会自然包含 compaction 节点。

**规则**: compaction entry 的 `entryId` 必须注册到 `parentById`。当前代码在解析所有 JSONL entries 时统一注册 `parentById.set(entryId, parentId)`，所以 compaction entries 自动包含。

**为什么**: 如果 compaction entry 不在 branch path 中，compactionSummary 会被 branch filter 过滤掉。

**ESLint**: 无自动检查（由 JSONL 解析结构保证）

## 修改 compaction 相关代码时的检查清单

1. ☐ `getFullMessages` 的两个解析分支是否同步更新？
2. ☐ compaction entry 是否同时 push 到 `allCompactionEntries` 和 `allMessages`？
3. ☐ streaming merge 的 dedup 是否基于 entryId 精确匹配？
4. ☐ `compaction_end` 的 streaming 分支是否 add 到 `compactionDeferredSessions`？
5. ☐ `agent_end` 是否检查 `compactionDeferredSessions` 并 flush？
6. ☐ 前端 `loadSessionMessages({ force: true })` 是否正确处理 compactionSummary？

## 测试文件

- `test/compaction-post-reload-integrity.test.ts` — 12 个测试覆盖工具调用合并、顺序、多 compaction
